---
name: jason-xhs-ai-share
description: "为 Jason 制作或修改小红书 AI 知识分享图文：简洁高级的蓝紫编辑风格、吸引点击且兑现承诺的文案、逐图内容校对、发布顺序与角标校验，并交付图片、文本和 ZIP。用户要求沿用这套风格、生成 AI 分享帖或修订已有卡片时使用。"
---

# Jason 小红书 AI 知识分享

把用户的 AI 工具经验、方法或观点做成可直接发布的图文素材。默认中文，采用已经确认的简洁蓝紫视觉。支持初版规划、整套制作、单页修改和最终打包；先判断本次请求属于哪一种，完成用户所需的阶段。

## 已确认的个人默认

- 风格：简洁、大气、高级；雾白、深墨蓝、钴蓝与蓝紫渐变；无衬线字、大标题、明确对齐、充足留白。使用 [视觉规范](references/visual-system.md)。
- 内容：先抓真实痛点，再给机制与方法，最后给可执行动作。以人愿意读下去为准，不堆知识点，不编造体验与效果。使用 [编辑规范](references/editorial-guide.md)。
- 默认 6 张，包含 1 张封面和 5 张正文；可按内容和用户要求调整数量，避免为凑页重复。
- **封面不编号。正文只在左上角从 01 连续编号。右下角不显示任何页码或总页数。** 文件名按上传顺序从 01 开始，文件序号包含封面，画面页码不包含封面。正文内的列表号、步骤号与角标不同，不随页码删除。
- 不加账号、水印、二维码。参考图只提取视觉规律，不复制原作者布局、文案、签名或标识。
- 用户已授权直接制作时，简要说明方案后继续，不重复征求风格、页数或出图确认。仅要求“初版规划”时只输出规划。缺失信息确实影响内容真实性时再问。
- 本次用户明确指令优先于这些默认。不要修改通用 baoyu skill 的全局偏好来实现个人风格。

## 与现有 skills 的关系

本 skill 根据 `baoyu-xhs-images` 的拆页、逐图提示词、封面风格锚点、版本留存与定点修订流程整理；本文件已经包含可独立执行的流程。若用户同时指定 baoyu 且它可用，可读取它辅助策划；本人的明确风格与编号要求覆盖其手绘风格、自动配色和双重编号。不要重复跑首次偏好设置。

在 Codex 中制作或修改位图时，读取并使用可用的 `imagegen` skill 与原生图像工具。检查实际工具参数，不臆造输出路径、批量或引用参数。无原生工具时说明情况并交付已完成文案与提示词，不把未生成的图片称为完成，不擅自切换付费 API。脚本只做清单和文件校验，不能在位图上覆盖文字来掩盖生成错误。

## 制作流程

1. **锁定意图。** 从上下文提取主题、受众、核心观点、痛点、预期收获、必须保留的说法和最新修改。区分用户观点、亲测记录、已验证产品事实、尚未验证的设想。需要时核查时效性事实。
2. **策划文案。** 先给出推荐发布标题与封面标题，再安排阅读顺序和每页唯一重点。写好图片上的完整文本及发布正文，检查标题承诺在后文有对应内容。不要先出图再临时补文案。
3. **建立单一清单。** 按 [核对契约](references/qa-contract.md) 建立 `manifest.json`，记录上传顺序、稳定页面 id、页头、全部可见正文、图片路径和提示词路径。角标由上传顺序推导。先运行计划检查。
4. **存提示词再出图。** 每张提示词写入文件，包含精确文案、派生页码、构图与不应出现的内容。新系列先生成并检查封面，再用通过的封面作为后续图片的风格参考。引用图不是新增文案来源；深浅底可变化。已生成页面的小改以该页最新版为编辑目标，不重新套用封面结构。
5. **逐图核对。** 查看实际生成的图片，按读图顺序逐字核对标题、正文、数字、品牌名、否定词、流程箭头、左上角及其他角落。记录观察到的内容和图片哈希。OCR 可辅助，但不能代替看图。看不清必须标记未通过，不能照抄提示词作为“观察结果”。
6. **整套核对。** 依上传顺序检查封面→正文的衔接、术语一致性、图文结论一致性、重复或缺失信息、正文编号连续性、所有右下角无页码、尺寸一致及最新版文件是否齐全。只修有问题的页，再检查该页与相关衔接。
7. **打包交付。** 所有检查通过后运行发布校验与 ZIP 打包。按上传顺序显示图片，提供 ZIP、可复制正文和可用本地路径。简要报告改动与实际检查，不把技术清单塞进帖子。

## 修改与版本规则

- 修改前查看目标图并确认它是最新版。先更新清单和本次提示词；保留旧图，输出新版本。
- 小修改锁住其余内容、排版、字号、配色和尺寸。重新核对整张图，防止“改角标时正文被改回旧版”。
- 插入、删除、重排图片时，重算全部上传顺序和正文页码；不要只改文件名。封面仍不计入正文编号。
- 针对明确问题继续修复；同一问题两次定点修复仍失败时，减少该页文本或调整必要的布局再试。不要无限重生成全套。确实无法完成则保留问题记录，不能将失败项标记通过。
- 每次改动清单或图片后，受影响的核对记录失效。必须重新看图和记录，不能只更新哈希延用旧结论。

## 默认交付

输出到当前工作区的 `image-cards/{topic}/release-vN/`：

- `images/01-cover.png`、`images/02-*.png`……：按上传顺序排序的正式图片。
- `caption.txt`：标题、正文、话题，可直接复制。
- `plan.md`：定位、推荐标题及理由、拆页顺序、视觉选择；在规划任务或新系列时提供。
- `manifest.json`、`review.json`、`qa-report.json`：文案与检查记录。
- `prompts/`：实际使用的完整逐图提示词；需要事实归因时附 `sources.md`。
- ZIP 只收正式图片、当前文案、当前提示词和检查记录，不混入原参考、旧版本或失败候选。

命令中的 `SKILL_DIR` 表示本文件所在目录，`RELEASE` 表示当前输出目录。校验脚本仅需 Python 3.9+ 标准库，不联网、不生成或修改图片：

```bash
python3 "$SKILL_DIR/scripts/check_release.py" "$RELEASE/manifest.json" --stage plan
python3 "$SKILL_DIR/scripts/check_release.py" "$RELEASE/manifest.json" --review "$RELEASE/review.json" --package "$RELEASE/release.zip"
```

脚本通过只表示机械检查与已记录的人工读图结果一致；不证明事实真实性或美观程度。事实与视觉结论必须来自实际审阅。
